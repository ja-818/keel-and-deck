{
  "name": "houston-registry",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute houston-registry --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute houston-registry --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "wrangler": "^4.81.0"
  }
}
