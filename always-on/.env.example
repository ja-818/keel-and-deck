## Bearer token clients must send when authenticating to the engine.
## Rotate if exposed. Desktop app stores its copy in ~/.houston/engine.json.
HOUSTON_ENGINE_TOKEN=replace-me-with-a-long-random-string
