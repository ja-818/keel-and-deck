---
name: run-variance-analysis
description: "Use when the user says 'why was opex up in March' / 'compare actuals to budget' / 'what drove the variance last month' — I compare period actuals against budget (if present), prior period, and same period prior year; decompose each material variance into price / volume / mix / one-time drivers grounded in specific JEs; and write a plain-English narrative. Reads journal-entries.json + prior P&L + `config/budget.json`; writes `variance-analyses/{YYYY-MM}.md` and appends to `outputs.json`."
---

# Run Variance Analysis

Actuals vs. budget vs. prior period vs. same-period prior year. Driver decomposition (price / volume / mix / one-time) on every material line. Each driver cite JE id or transaction set. Narrative focus 3-5 biggest movers — user see story, not spreadsheet.

## When to use

- "why was opex up in March" / "what drove revenue miss".
- "compare actuals to budget for {period}".
- "run variance analysis on {period}".
- Called by `run-monthly-close` after `generate-financial-statements statement=pnl` wrote current-period P&L.

## Steps

1. **Read context.** Load `context/bookkeeping-context.md`, `config/context-ledger.json` (for `domains.budget` — cadence + path), `config/chart-of-accounts.json`. Read `config/budget.json` if exists (`[{period, glCode, amount, note?}]`).

2. **Pick comparison baselines.** For requested `period` (`YYYY-MM` or `YYYY-QN`), assemble up to three baselines:
   - **Budget** — rows from `config/budget.json` for period. If absent, skip + note "no budget on file".
   - **Prior period** — `financials/{prior-YYYY-MM}/pnl.md` if present; else recompute on fly from `journal-entries.json`.
   - **Same period prior year** — `financials/{prior-YYYY-MM-12}/pnl.md` if present; else recompute.

3. **Load actuals for period.** Read period P&L from `financials/{YYYY-MM}/pnl.md` (generated by `generate-financial-statements`). If missing, recompute from `journal-entries.json` on fly + note canonical P&L not yet written.

4. **Compute variances per GL line.** For each GL line in any of: actuals, budget, prior period, prior year, compute:
   - `actual_minus_budget`, `pct_vs_budget`
   - `actual_minus_prior_period`, `pct_vs_prior_period`
   - `actual_minus_prior_year`, `pct_vs_prior_year`

5. **Apply materiality threshold.** Default: `abs(variance) > 5% AND abs(variance) > $1000`. Configurable per run via arg. Only material variances get driver decomposition. Non-material summarized in single table at bottom.

6. **Decompose each material variance into drivers.** Ground each driver in specific JEs or transactions:
   - **Price** — same-quantity unit cost changed (e.g., SaaS vendor price increase). Cite JE ids where new price first appears.
   - **Volume** — more or fewer units at same unit price (e.g., more hosting spend because usage doubled). Cite transaction count vs. baseline + representative JE ids.
   - **Mix** — different blend of SKUs / vendors / categories. Cite entrant JEs + exited vendors.
   - **One-time** — non-recurring (true-up, one-off refund, annual renewal booked in month). Cite JE id + memo.

   Each driver has: `{driver, amount, jeRefs: [id…], transactionRefs?: [ids…], narrative}`. `amount` must sum to total variance within $1.00. Unexplained residual logged explicitly — never silently absorbed.

7. **Write plain-English narrative on 3-5 biggest movers.** Narrative names each mover, dollar impact, primary driver, specific evidence (JE id or vendor or transaction set). No invented causes. Thin evidence → say so. "no obvious driver, recommend user review" acceptable. Speculation padding not.

8. **Write variance artifact.** Path: `variance-analyses/{YYYY-MM}.md`. Atomic write: `.tmp` → rename. Structure:
   - Header: period, baselines used, materiality threshold, accounting method.
   - **Headline** — 1-2 sentence summary (e.g., "Opex +$45k (+12%) vs. budget, driven by hosting doubling and a one-time legal true-up").
   - **Narrative** — 3-5 paragraphs on biggest movers, each citing JE ids / vendors / transaction counts.
   - **Material variances table** — one row per material GL line with actuals, each baseline, variance, driver decomposition.
   - **Non-material variances** — compact summary table.
   - **Unexplained residuals** — any driver decomposition not tied back within $1.00.
   - Footer: sources (P&L file path, budget file path, prior-period P&L paths, journal-entries.json hash).

9. **Append to `outputs.json`.** Read-merge-write. Row: `{id, type: "variance-analysis", title: "Variance — {YYYY-MM}", summary: "<the headline>", path: "variance-analyses/{YYYY-MM}.md", status: "draft", domain: "reporting"}`.

10. **Summarize to user.** One paragraph: headline + 3-5 biggest movers with dollar impact + primary driver, plus unexplained residuals needing review. Point at written file.

## Outputs

- `variance-analyses/{YYYY-MM}.md`
- `outputs.json` row: `type: "variance-analysis"`, `domain: "reporting"`, `status: "draft"` until user signs off.