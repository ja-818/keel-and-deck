[package]
name = "houston-memory"
version = "0.2.0"
edition = "2021"
description = "Character-bounded persistent memory for AI agents (Hermes model)"
license = "MIT"

[dependencies]
thiserror = "2"

[dev-dependencies]
tempfile = "3"
